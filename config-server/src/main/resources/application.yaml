server:
  port: 8888
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GustavoSC1/Event-Management-Config-Files
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'config*'  
